# Скопируй этот файл в .env и заполни своими реальными значениями
# .env НИКОГДА не должен попадать в Git — он уже в .gitignore

BOT_TOKEN=your_bot_token_here
DATABASE_URL=postgresql://user:password@localhost:5432/sober_tracker
PORT=3000
NODE_ENV=development
